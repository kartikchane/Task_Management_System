import {useCallback,useEffect,useState} from 'react';export function useRefresh(fn){const cb=useCallback(fn,[fn]);useEffect(()=>{cb();const h=()=>cb();window.addEventListener('tf-refresh',h);return()=>window.removeEventListener('tf-refresh',h)},[cb])}export function useAsync(initial=[]){const [data,setData]=useState(initial),[loading,setLoading]=useState(true);return {data,setData,loading,setLoading}}